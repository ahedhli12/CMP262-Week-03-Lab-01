# CMP 262 – Data Science Programming

## Week 3 – Lab 1: Lists and Tuples

**Due:**  
**Points: 100**

## Purpose

This assignment introduces Python **lists** and **tuples**, two important data structures used to store multiple values.

Complete all work in a Jupyter Notebook using Visual Studio Code.

Use the notebook provided in this repository:

`ListsAndTuples.ipynb`

---

## Part 1 – Creating and Accessing Lists

Create a list named `courses` that contains at least five course names.

Display:

- The entire list
- The first course
- The third course
- The last course
- The number of courses in the list using `len()`

**Points: 15**

---

## Part 2 – Modifying a List

Create a list named `favorite_foods` with at least four foods.

Complete the following tasks:

1. Display the original list.
2. Change one item in the list.
3. Add one new food using `append()`.
4. Insert one new food using `insert()`.
5. Remove one food using `remove()` or `pop()`.
6. Display the final list.

**Points: 20**

---

## Part 3 – Working with Numbers in a List

Create a list named `scores` that contains at least six numeric values.

Display:

- The entire list
- The highest score using `max()`
- The lowest score using `min()`
- The total of all scores using `sum()`
- The average score

Calculate the average using:

`sum(scores) / len(scores)`

**Points: 20**

---

## Part 4 – Looping Through a List

Create a list named `cities` containing at least five cities.

Use a `for` loop to display each city on a separate line.

Then create a second loop that displays a message for each city in this format:

`I would like to visit CITY.`

Replace `CITY` with the value from the list.

**Points: 15**

---

## Part 5 – Tuples

Create a tuple named `student_record` containing:

- A student name
- A major
- A graduation year

Display:

- The entire tuple
- The student's name
- The student's major
- The graduation year

Then use `len()` to display the number of values in the tuple.

**Points: 15**

---

## Part 6 – Lists vs. Tuples

In a **Markdown cell**, answer the following questions in your own words:

1. What is one similarity between a list and a tuple?
2. What is the main difference between a list and a tuple?
3. Which one is mutable?
4. Which one is immutable?
5. Give one example of when you might use a tuple instead of a list.

**Points: 15**

---

## Submission

Before submitting:

1. Run all notebook cells.
2. Make sure there are no errors.
3. Make sure all required output is visible.
4. Save the notebook as `ListsAndTuples.ipynb`.
5. Commit and push your completed work to GitHub.
6. Submit the link to your completed repository through Blackboard Ultra.

## Important

- Write your own code.
- Use clear variable names.
- Add a Markdown heading before each part.
- Test each section before moving to the next one.
- Make sure you understand the difference between lists and tuples.

## Grading Summary

| Section | Points |
|---|---:|
| Creating and Accessing Lists | 15 |
| Modifying a List | 20 |
| Numbers in a List | 20 |
| Looping Through a List | 15 |
| Tuples | 15 |
| Lists vs. Tuples | 15 |
| **Total** | **100** |
